#!/bin/sh

if [ $# -eq 0 ]
  then
    echo "help: ./test [PATH (relativa o assoluta) della cartella public_html]"
else
    proj_dir=$(echo $(pwd)/src/views)
    public_path=$1
    
    cd $public_path
    for file in `ls $proj_dir`; do
	ln -s $proj_dir/$file
    done
    rm template;
fi
